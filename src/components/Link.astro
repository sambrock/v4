---
import type { HTMLAttributes } from 'astro/types';

export interface Props extends HTMLAttributes<'a'> {}

const { ...attrs } = Astro.props;
---

<a
  {...attrs}
  class={`inline-flex items-center gap-1 text-[#e8e8e8cc] hover:text-[#e8e8e8] transition-colors ${attrs.class}`}
  ><slot />{attrs.target === '_blank' && <span class="text-[12px]">↗</span>}</a
>
